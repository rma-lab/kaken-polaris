# Python / uv
.venv/
__pycache__/
*.py[cod]

# macOS
.DS_Store

# 参考資料の原本と抽出テキストは git に入れない（大きい／権利が出所ごとに異なる）。
# 出所 URL は各 reference/*/index.md にあり、JSPS 公開資料は reference/fetch_jsps.sh で再取得できる。
reference/**/*.pdf
reference/**/*.txt
reference/**/*.xlsx
reference/seminar/**
!reference/seminar/index.md
# JSPS の様式（公開・小さい）と、そこから作った構造化データは追跡する
!reference/yoshiki/2027/*.docx

# セミナー資料の分析（主張抽出・統合）は、出所の許諾・確認が済むまで公開リポジトリに入れない。
# 手元では analysis/ に置いたまま作業する。
analysis/

# 生成物のプレビュー画像
reports/flyer/preview*.png
